[project]
name = "base-repo"
version = "0.1.0"
# Commented out to force Dependabot to classify this project as an application
# description = "Base repository with uv for dependency management"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "yamllint>=1.38.0",
]
ci = [
    "uv>=0.12.5",
]

[tool.uv]
exclude-newer = "3 days"
